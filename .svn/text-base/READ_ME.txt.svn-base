virtualenv stage

pip install django
pip install  django-push-notifications 
pip install django-uuidfield
pip install psycopg2
pip install gunicorn


Run the db backup command just to create the dfault dataase with categories

To take the back up Command is pg_dump dbname > outfile
eg:   pg_dump iot > db_backup


To restore the backup file

psql dbname < infile
